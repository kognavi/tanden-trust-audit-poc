name: Semgrep

on:
  pull_request: {}
  push:
    branches: ["main"]
  schedule:
    - cron: '0 3 * * 1'  # 毎週月曜3時(UTC)に定期スキャン

jobs:
  semgrep:
    name: Semgrep Scan
    runs-on: ubuntu-latest
    container:
      image: semgrep/semgrep
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Run Semgrep
        run: semgrep ci
        env:
          # OSS向け公開ルールセットを使用（無料・トークン不要）
          SEMGREP_RULES: >-
            p/security-audit
            p/secrets
            p/owasp-top-ten
            p/javascript
